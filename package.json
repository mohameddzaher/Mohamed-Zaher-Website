{
  "name": "mohamedzaher-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Mohamed Zaher — personal portfolio + corporate platform + client SaaS portal.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:*",
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:backend": "npm --workspace backend run dev",
    "build": "npm --workspace frontend run build && npm --workspace backend run build",
    "start:frontend": "npm --workspace frontend run start",
    "start:backend": "npm --workspace backend run start",
    "lint": "npm --workspace frontend run lint && npm --workspace backend run lint",
    "seed": "npm --workspace backend run seed"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=20"
  }
}
