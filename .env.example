# ========= ROOT — copy to .env and fill =========
# This is shared scaffolding. Frontend and backend each have their own .env.example
# inside their folders with the variables they actually consume.

NODE_ENV=development
